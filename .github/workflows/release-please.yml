name: release-please
on:
  push:
    branches:
      - release_please_secret
  schedule:
    - cron: '1 0 * * *'

permissions:
  contents: write
  pull-requests: write

jobs:
  common:
    uses: City-of-Helsinki/.github/.github/workflows/release-please.yml@release_please_workflow-RAT-341
